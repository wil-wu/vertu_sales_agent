# LoveActually.AI 情感系统 - 快速总览

> 基于 `prompt_analysis.md` 提炼的 Mermaid 流程图，便于快速了解优/劣势

---

## 1. 系统整体架构流程图

```mermaid
flowchart TB
    subgraph Input["📥 用户输入"]
        U1[语音/文字输入]
    end

    subgraph Layer1["🔀 第一层: 意图识别"]
        ICE[Intent Classification Engine<br/>意图分类引擎]
    end

    subgraph Layer2["👤 第二层: 用户画像构建"]
        A[Agent 1: Astute<br/>AI Matchmaker<br/>语音收集+人格分析]
        PA[Agent 3: Profile Analyst<br/>9大维度标签抽取]
        BA[Agent 4: Behavioral Analyst<br/>心理画像+JSON结构化]
    end

    subgraph Layer3["💬 第三层: 对话与输出"]
        LK[Agent 5: Loving Kitty<br/>日常聊天伴侣]
        LR[Agent 6: Love Recorder<br/>AI Report+破冰建议]
    end

    subgraph Layer4["🧠 第四层: 记忆持久化"]
        DA[Agent 7: Data Analyst<br/>长期记忆增量更新]
    end

    U1 --> ICE
    ICE -->|dating_coach| A
    ICE -->|ai_matchmaker| PA
    ICE -->|general_chat| LK
    A --> BA
    BA --> LR
    LK --> DA
    LR --> DA
    PA --> DA
```

---

## 2. 用户意图路由流程

```mermaid
flowchart LR
    subgraph 意图分类["Intent Classification Engine"]
        direction TB
        I1[dating_coach<br/>战术建议/优化简介/聊天分析]
        I2[ai_matchmaker<br/>兼容性分析/评判资料]
        I3[general_chat<br/>闲聊/问候/情绪宣泄]
    end

    Input[用户消息] --> 意图分类
    意图分类 --> R1[Astute + Profile Analyst]
    意图分类 --> R2[Loving Kitty]
    意图分类 --> R3[Behavioral Analyst]
```

---

## 3. 核心设计模式对比

```mermaid
mindmap
  root((LoveActually.AI<br/>设计模式))
    角色锚定
      视觉身份
      人格特质
      功能描述
    反 AI 训练
      禁止 Markdown
      禁止列表结构
      禁止 AI 常用语
      口语化/碎片化
    结构化输出
      JSON Schema
      无解释
      无代码块
    记忆系统
      增量更新
      覆盖冲突
      推理补充
```

---

## 4. 优劣分析一览

```mermaid
flowchart TB
    subgraph 优势["✅ 优势 (Strengths)"]
        S1["多 Agent 专业化分工<br/>7 个专职 Agent 各司其职"]
        S2["意图路由前置<br/>先分类再处理，提高效率"]
        S3["反 AI 训练<br/>消除 AI 味，更自然对话"]
        S4["严格 JSON 输出<br/>便于系统集成与解析"]
        S5["增量记忆机制<br/>长期画像持续更新"]
        S6["9 维用户画像<br/>多维度而非简单标签"]
        S7["角色锚定<br/>具象化视觉身份+语调"]
        S8["语言一致性<br/>输出跟随输入语言"]
    end

    subgraph 劣势["❌ 劣势 / 考量点 (Considerations)"]
        W1["系统复杂度高<br/>7 Agent 协同，维护成本大"]
        W2["Prompt 依赖性强<br/>任一 Agent 质量影响整体"]
        W3["意图分类准确性<br/>误分类导致错误路由"]
        W4["JSON Schema 维护<br/>多套结构需长期同步"]
        W5["潜在延迟<br/>意图识别→路由→处理 链路较长"]
        W6["去重/冲突规则复杂<br/>需严格避免幻觉"]
    end

    S1 -.-> 优势
    S2 -.-> 优势
    S3 -.-> 优势
    S4 -.-> 优势
    S5 -.-> 优势
    S6 -.-> 优势
    S7 -.-> 优势
    S8 -.-> 优势

    W1 -.-> 劣势
    W2 -.-> 劣势
    W3 -.-> 劣势
    W4 -.-> 劣势
    W5 -.-> 劣势
    W6 -.-> 劣势
```

---

## 5. 反 AI 训练规则 (Loving Kitty)

```mermaid
flowchart LR
    subgraph 禁止["❌ 禁止"]
        B1["# ## ### 标题"]
        B2["1. 2. 3. 列表"]
        B3["看起来... 很高兴..."]
        B4["😊😊 重复 emoji"]
    end

    subgraph 推荐["✅ 推荐"]
        G1["短促碎片化"]
        G2["反应优先于回答"]
        G3["适当小写/俚语"]
        G4["0-1 emoji/条"]
    end

    禁止 --> 推荐
```

---

## 6. Vertu Sales Agent 可借鉴映射

```mermaid
flowchart TB
    subgraph LoveActually["LoveActually 设计"]
        LA1[Astute: 人格分析]
        LA2[Intent Engine]
        LA3[Profile Analyst]
        LA4[Loving Kitty]
    end

    subgraph Vertu["Vertu 销售场景映射"]
        V1[Technical Advisor<br/>技术咨询]
        V2[Intent: 询价/技术支持/投诉/闲聊]
        V3[用户画像: 产品偏好/价格敏感度]
        V4[自然口语化销售话术]
    end

    LA1 --> V1
    LA2 --> V2
    LA3 --> V3
    LA4 --> V4
```

---

## 📋 快速速查表

| 维度 | 优势 | 劣势 |
|------|------|------|
| **架构** | 7 Agent 专业化、意图路由前置 | 复杂度高、维护成本大 |
| **对话** | 反 AI 训练、口语化、角色锚定 | Prompt 质量高度敏感 |
| **数据** | JSON 严格输出、增量记忆、9 维画像 | Schema 维护、冲突规则复杂 |
| **体验** | 语言一致、自然碎片化 | 意图误分类影响路由 |
